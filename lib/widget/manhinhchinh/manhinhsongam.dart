import 'package:flutter/material.dart';

class ManhinhSongam extends StatelessWidget {
  const ManhinhSongam({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('song am'),
    );
  }
}
